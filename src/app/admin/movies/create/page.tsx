"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { ImageField, VideoField } from "@/components/admin/MediaField";

export default function CreateMoviePage() {
	const router = useRouter();
	const [form, setForm] = useState({ title: "", slug: "", description: "", poster_url: "", backdrop_url: "", video_url: "", director: "", cast_members: "", quality: "", release_year: "", rating: "", status: "draft", featured: false });
	const [error, setError] = useState("");
	function update(key: string, value: string | boolean) { setForm((current) => ({ ...current, [key]: value })); }
	async function submit(event: React.FormEvent) {
		event.preventDefault();
		const response = await fetch("/api/admin/movies", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...form, release_year: form.release_year || null, rating: form.rating || null }) });
		if (!response.ok) { const body = await response.json(); setError(body.error ?? "Unable to create movie"); return; }
		router.push("/admin/movies");
	}
	return <main className="py-4"><div className="mb-8"><p className="text-sm font-semibold uppercase tracking-[0.2em] text-brand-400">Content</p><h1 className="mt-2 text-4xl font-semibold text-white">Add movie</h1><p className="mt-3 text-muted">Only published titles appear in the public catalog.</p></div><form onSubmit={submit} className="card-surface-elevated grid gap-5 p-6 md:grid-cols-2">{[["title","Title"],["slug","Slug"],["release_year","Release year"],["rating","Rating"]].map(([key, label]) => <label className="grid gap-2 text-sm font-medium text-white" key={key}>{label}<input required={key === "title" || key === "slug"} className="rounded-button border border-white/10 bg-black/20 px-3 py-2 text-white" value={form[key as keyof typeof form] as string} onChange={(event) => update(key, event.target.value)} /></label>)}<ImageField label="Poster" folder="movies" value={form.poster_url} onChange={(value) => update("poster_url", value)} /><ImageField label="Backdrop" folder="movies" value={form.backdrop_url} onChange={(value) => update("backdrop_url", value)} /><VideoField label="Video / embed URL" folder="movies" value={form.video_url} onChange={(value) => update("video_url", value)} /><label className="grid gap-2 text-sm font-medium text-white">Director<input className="rounded-button border border-white/10 bg-black/20 px-3 py-2 text-white" value={form.director} onChange={(event) => update("director", event.target.value)} /></label><label className="grid gap-2 text-sm font-medium text-white">Quality badge<input className="rounded-button border border-white/10 bg-black/20 px-3 py-2 text-white" placeholder="HD, LOW QUALITY, 4K…" value={form.quality} onChange={(event) => update("quality", event.target.value)} /></label><label className="grid gap-2 text-sm font-medium text-white md:col-span-2">Cast (comma separated, shown on the detail page)<textarea className="min-h-20 rounded-button border border-white/10 bg-black/20 px-3 py-2 text-white" value={form.cast_members} onChange={(event) => update("cast_members", event.target.value)} /></label><label className="grid gap-2 text-sm font-medium text-white md:col-span-2">Description<textarea className="min-h-32 rounded-button border border-white/10 bg-black/20 px-3 py-2 text-white" value={form.description} onChange={(event) => update("description", event.target.value)} /></label><label className="grid gap-2 text-sm font-medium text-white">Status<select className="rounded-button border border-white/10 bg-black/20 px-3 py-2 text-white" value={form.status} onChange={(event) => update("status", event.target.value)}><option value="draft">Draft</option><option value="published">Published</option><option value="archived">Archived</option></select></label><label className="flex items-center gap-3 self-end text-sm font-medium text-white"><input type="checkbox" checked={form.featured} onChange={(event) => update("featured", event.target.checked)} /> Feature on homepage</label><div className="md:col-span-2 flex items-center gap-4"><button className="rounded-button bg-brand-600 px-5 py-3 font-semibold text-white" type="submit">Create movie</button>{error && <p className="text-sm text-red-300">{error}</p>}</div></form></main>;
}
